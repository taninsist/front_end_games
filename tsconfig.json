{
  "compilerOptions": { //编译选项
    "target": "es5", //配置编译目标代码的版本标准
    "module": "esnext", //配置编译目标使用的模块化标准
    "lib": [
      "es2016",
      "DOM"
    ], //这里没有node环境可以配置，但是去掉了浏览器环境，
    // 也不知道这是node环境，所以导致console也没有了，这里必须要安装第三方库(@types/node)
    "sourceMap": true, //代码地图
    "strict": true, //启用严格的类型检查
    "removeComments": true, //移除注释
    "esModuleInterop": true, //用es6的模块化标准和非es6的模块化标准进行交互
    "moduleResolution": "node", //模块解析方式，
    "isolatedModules": true, //每个单独的文件必须是一个模块
  },
  "include": [
    "./src"
  ] //要编译的文件夹。默认整个工程
}